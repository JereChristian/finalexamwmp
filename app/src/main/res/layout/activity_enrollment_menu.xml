<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="32dp"
    android:gravity="center">

    <!-- Title of Enrollment Menu -->
    <TextView
        android:id="@+id/enrollmentMenuTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Select Subjects to Enroll"
        android:textSize="20sp"
        android:gravity="center"
        android:layout_marginBottom="20dp" />

    <!-- List of subjects available for enrollment (RecyclerView or ListView) -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/subjectsListView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20dp"/>

    <!-- Button to confirm the enrollment -->
    <Button
        android:id="@+id/enrollButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Confirm Enrollment"
        android:layout_marginTop="20dp" />

    <!-- Button to go back to Home Activity -->
    <Button
        android:id="@+id/goBackButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Go Back"
        android:layout_marginTop="20dp" />

</LinearLayout>
